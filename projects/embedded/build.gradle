plugins {
    id 'java-library'
}

description = "HoloDB embedded functionality"

tasks.withType(JavaCompile).configureEach {
    options.release = 8
}

dependencies {
    implementation project(':bootstrap')
}
